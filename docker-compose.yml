version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - FLASK_APP=flask_app.py
    volumes:
      - ./data:/app/data
    restart: unless-stopped
